to install pip install -r requirements.txt then run the file
to toggle the gui, use win+space. Esc closes it.

Current commands:
-open:
  simply runs an app


future plans:
-incorporate my old jarvis into this to make it nicer and maybe cooler who knows i sure don't
